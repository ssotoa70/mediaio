{
  "name": "mediaio",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "prettier --check \"src/**/*.ts\"",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "prettier": "^3.4.2",
    "tsx": "^4.8.2",
    "typescript": "^5.4.5",
    "vitest": "^2.1.3"
  }
}
